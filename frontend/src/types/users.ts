export interface UserData {
  id: string;
  name: string;
  email: string;
  models: [];
  createdAt: string;
}

export interface User {
  id: string;
  name: string;
  email: string;
  createdAt: string;
}